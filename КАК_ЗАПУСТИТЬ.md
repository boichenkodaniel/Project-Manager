# 🚀 Как запустить и войти в систему

## Быстрая инструкция

### 1️⃣ Подготовка базы данных

Выполните SQL скрипт для создания таблиц:

```bash
psql -U postgres -d Project-manager -f database_update.sql
```

Или через psql:
```sql
\c Project-manager
\i database_update.sql
```

### 2️⃣ Создание первого администратора

**Способ 1: Через PHP скрипт (самый простой)**

```bash
php create_admin.php
```

Это создаст администратора:
- **Логин:** `admin`
- **Пароль:** `admin123`
- **Email:** `admin@example.com`

**Способ 2: Через SQL**

```sql
INSERT INTO "User" (fullname, email, login, password, role)
VALUES (
    'Администратор',
    'admin@example.com',
    'admin',
    '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi',
    'Администратор'
);
```

> Пароль для этого хеша: `admin123`

### 3️⃣ Установка зависимостей

```bash
npm install
```

### 4️⃣ Запуск серверов

**Вариант А: Одной командой (рекомендуется)**

```bash
npm run dev:full
```

Это запустит:
- ✅ PHP сервер на `http://localhost:8000`
- ✅ Vite dev server на `http://localhost:3000`

**Вариант Б: Раздельно (в двух терминалах)**

Терминал 1:
```bash
npm run backend
```

Терминал 2:
```bash
npm run dev
```

### 5️⃣ Вход в систему

1. Откройте браузер
2. Перейдите на: **http://localhost:3000/login.html**
3. Введите данные:
   - **Логин:** `admin`
   - **Пароль:** `admin123`
4. Нажмите "Войти"

После успешного входа вы попадёте на главную страницу!

---

## ✅ Проверка работы

После входа вы сможете:

1. **Управлять пользователями** (кнопка "+" → вкладка "User")
   - Создавать новых пользователей
   - Назначать роли: Администратор, Руководитель проектов, Исполнитель, Клиент

2. **Создавать проекты** (кнопка "+" → вкладка "Project")
   - Только для администраторов и руководителей проектов

3. **Создавать задачи** (кнопка "+" → вкладка "Task")
   - Только для администраторов и руководителей проектов

4. **Просматривать данные** в зависимости от роли

---

## 🔧 Устранение проблем

### Ошибка подключения к БД
```
Ошибка подключения к БД: ...
```
**Решение:**
- Проверьте, что PostgreSQL запущен
- Проверьте настройки в `src/backend/config/db.php`
- Убедитесь, что база `Project-manager` существует

### Страница не открывается
**Решение:**
- Убедитесь, что оба сервера запущены (PHP и Vite)
- Проверьте порты: PHP на 8000, Vite на 3000
- Откройте консоль браузера (F12) для просмотра ошибок

### Ошибка 401 (Не авторизован)
**Решение:**
- Убедитесь, что пользователь создан в БД
- Проверьте правильность логина и пароля
- Проверьте, что сессии PHP работают (проверьте права на папку для сессий)

### Не могу создать пользователя
**Решение:**
- Убедитесь, что вы вошли как администратор
- Проверьте консоль браузера на ошибки
- Проверьте Network вкладку в DevTools

---

## 📝 Полезные команды

### Проверить пользователей в БД
```sql
SELECT id, fullname, email, login, role FROM "User";
```

### Изменить пароль администратора
```sql
-- Сначала сгенерируйте новый хеш пароля в PHP:
-- <?php echo password_hash('новый_пароль', PASSWORD_DEFAULT); ?>

UPDATE "User" 
SET password = 'новый_хеш_пароля'
WHERE login = 'admin';
```

### Проверить, что серверы запущены
- PHP: откройте http://localhost:8000/api?action=auth.me
- Vite: откройте http://localhost:3000

---

## 🎯 Что дальше?

1. **Создайте других пользователей** с разными ролями
2. **Создайте проект** и назначьте клиента
3. **Создайте задачи** и назначьте исполнителей
4. **Проверьте уведомления** - они будут появляться при создании задач и проектов
5. **Создайте issues** от имени исполнителя

Удачи! 🚀

